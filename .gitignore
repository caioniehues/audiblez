venv
__pycache__
.idea
chapters.txt
.DS_Store
*.pyc
*.wav
*.mp3
*.epub
# Exception: the tiny in-repo e2e fixture must be committed so CI's
# "create audiobook" step has an EPUB in a fresh clone (see git-clone-and-run.yml).
!test/poe.epub
*.sh
*.json
*.onnx
*.npy
*.csv
*.log
*.sig
*.failed.jsonl
.audiblez_cache/
test/e2e_out/
dist
.venv
.direnv
.envrc
*.m4b

# Compound Engineering local config (machine-local, not committed)
.compound-engineering/*.local.yaml

# planning-with-files scaffold (local working memory, never committed)
.planning/
